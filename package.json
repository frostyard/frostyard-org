{
  "name": "frostyard-org",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/sync-extensions.mjs",
    "dev": "astro dev",
    "check": "astro check",
    "build": "astro build",
    "build:check": "astro build",
    "check:site": "node scripts/check-site.mjs",
    "ci": "npm run check && npm run build:check && npm run check:site",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/cloudflare": "12",
    "astro": "^5.13.0",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "wrangler": "^4.111.0"
  }
}
